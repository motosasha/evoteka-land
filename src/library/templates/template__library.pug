//- Library page template

include mixins.pug

- var isMainPage = false
- var pageTitle = "title"
- var metaDescription = "description"
- var metaKeywords = "keywords"
- var json = locals
- var path = pugPaths
- var isProjectNav = isProjectNav
- var isProjectLibrary = isProjectLibrary

block variables

doctype html
html(class="no-js", lang="ru")
  include ../../templates/template__default-head.pug

  body
    noscript У вас отключен JavaScript. Это пугает.

    if isProjectNav
      include ../../service/project-nav.pug

    if isProjectLibrary
      include ../blocks/library-trigger/library-trigger.pug

    .page
      .page__header
        +library-header
      .page__main
        block content
          .content-filler content-filler
      .page__footer
        +library-footer

    block templates

    //- script(src= path.js + "bundle.js")

    block scripts
